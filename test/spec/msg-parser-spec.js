/* global msgParser */
(function () {
    'use strict';

    describe('msgParser', function () {
        describe('getLinks', function () {
            it('should fail if some of the provided messages do not contain links', function () {
                
            });

            it('should fail if some of the provided messages contain invalid links', function () {

            });

            it('should succeed if all of the provided messages have correct links', function () {

            });
        });
    });
}());